version: "3"
services:
  jekyll:
    command: jekyll serve --force-polling --livereload
    image: jekyll/jekyll:latest
    volumes:
      - .:/srv/jekyll
      - ./vendor/bundle:/usr/local/bundle
    ports:
      -  4000:4000
      - 35729:35729